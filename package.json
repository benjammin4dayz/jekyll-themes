{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "rollup": "rollup -c --bundleConfigAsCjs"
  },
  "devDependencies": {
    "rollup": "^3.28.1",
    "@rollup/plugin-terser": "^0.4.3"
  }
}
